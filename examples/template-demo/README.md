# Demo template

## Dependencies

#### 1. `flip-link`:

```console
$ cargo install flip-link
```

#### 2. `probe-run`:

``` console
$ # make sure to install v0.3.0 or later
$ cargo install probe-run
```

## Demo description

Text text